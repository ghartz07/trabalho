#pragma once
#include <iostream>
#include <string>

using std::cout;
using std::endl;
using std::cin;
using std::getline;
//wrap --> if menu return (x), clean screen, call x class, call menu again
class Menu
{
public:
	int draw_menu();
};

